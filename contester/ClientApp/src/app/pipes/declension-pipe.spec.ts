import { DeclensionPipe } from './declension-pipe';

describe('DeclensionPipe', () => {
  it('create an instance', () => {
    const pipe = new DeclensionPipe();
    expect(pipe).toBeTruthy();
  });
});
